# Archive History Docs

**Purpose:** 保存从 `docs/history/` 退役后的按日记录，确保 raw provenance 被保留，但不与活跃恢复面竞争。

**Last updated:** 2026-04-17

## Scope

1. 已归档的 `YYYY-MM-DD.md` daily notes。

## Maintenance Rule

只有在 lifecycle 元数据同步后，才把 dated history 从 active tree 移到这里。
